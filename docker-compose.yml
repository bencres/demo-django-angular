services:
  backend:
    build: ./backend
    volumes:
      - db:/app/data
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    ports:
      - "4200:80"
    depends_on:
      - backend

volumes:
  db:
